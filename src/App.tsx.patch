*** Begin Patch
*** Update File: src/App.tsx
@@
- async function saveAllDiscovered(){ if(!discover?.results?.length)return; setBusy(true); const candidates=discover.results.map((item:any)=>({owner_id:session.user.id,name:String(item.company||item.title||'Empresa encontrada').slice(0,180),company:String(item.company||item.title||'').slice(0,180)||null,email:item.email||null,phone:item.phone||null,source:'busca_publica',source_url:item.url||null,source_detail:item.snippet||'Empresa encontrada em busca pública',status:'novo' as const,score:scoreLead({company:item.company||item.title,email:item.email,phone:item.phone,source:'busca_publica'}),dedupe_key:dedupeKey({company:item.company||item.title,email:item.email,phone:item.phone,name:item.company||item.title})})); const {data,error}=await supabase.from('leads').upsert(candidates,{onConflict:'owner_id,dedupe_key',ignoreDuplicates:true}).select(); setBusy(false); if(error)setMsg(error.message); else{setMsg(`${data?.length||0} empresa(s) nova(s) adicionada(s) ao CRM.`);await load();} }
+ async function saveAllDiscovered(){ const client=supabase; if(!client||!session||!discover?.results?.length)return; setBusy(true); const candidates=discover.results.map((item:any)=>({owner_id:session.user.id,name:String(item.company||item.title||'Empresa encontrada').slice(0,180),company:String(item.company||item.title||'').slice(0,180)||null,email:item.email||null,phone:item.phone||null,source:'busca_publica',source_url:item.url||null,source_detail:item.snippet||'Empresa encontrada em busca pública',status:'novo' as const,score:scoreLead({company:item.company||item.title,email:item.email,phone:item.phone,source:'busca_publica'}),dedupe_key:dedupeKey({company:item.company||item.title,email:item.email,phone:item.phone,name:item.company||item.title})})); const {data,error}=await client.from('leads').upsert(candidates,{onConflict:'owner_id,dedupe_key',ignoreDuplicates:true}).select(); setBusy(false); if(error)setMsg(error.message); else{setMsg(`${data?.length||0} empresa(s) nova(s) adicionada(s) ao CRM.`);await load();} }
*** End Patch
