import type { VercelRequest, VercelResponse } from '@vercel/node';
function clean(s:string){return s.replace(/\s+/g,' ').trim()}
function okUrl(raw:string){try{const u=new URL(raw); if(!['http:','https:'].includes(u.protocol)) return false; const h=u.hostname.toLowerCase(); if(['localhost','127.0.0.1','0.0.0.0','::1'].includes(h)||h.endsWith('.local')) return false; return true}catch{return false}}
function contacts(html:string){const emails=[...html.matchAll(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/ig)].map(m=>m[0].toLowerCase()).filter(e=>!e.includes('example.com')); const phones=[...html.matchAll(/(?:\+?55[\s.-]?)?(?:\(?\d{2}\)?[\s.-]?)?(?:9\d{4}|\d{4})[\s.-]?\d{4}/g)].map(m=>m[0].replace(/\D/g,'')); return {emails:[...new Set(emails)].slice(0,20),phones:[...new Set(phones)].filter(p=>p.length>=10).slice(0,20)}}
async function get(url:string){const r=await fetch(url,{headers:{'user-agent':'Mozilla/5.0 (compatible; LeadcheckBusinessScanner/1.0)','accept':'text/html,application/xhtml+xml'},redirect:'follow'}); return {r,text:r.ok?(await r.text()).slice(0,1500000):''}}
export default async function handler(req:VercelRequest,res:VercelResponse){if(req.method!=='POST')return res.status(405).json({error:'POST only'});const url=String(req.body?.url||'');if(!okUrl(url))return res.status(400).json({error:'URL pública inválida'});try{const first=await get(url);if(!first.r.ok){return res.status(200).json({ok:false,blocked:true,url,status:first.r.status,error:`O site bloqueou o scanner (HTTP ${first.r.status}). Use “Buscar empresas reais” para encontrar a empresa por busca pública.`})}const html=first.text;const c=contacts(html);const title=clean((html.match(/<title[^>]*>([\s\S]*?)<\/title>/i)?.[1]||'').replace(/<[^>]+>/g,''));const textContent=clean(html.replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ')).slice(0,1000);return res.json({ok:true,url: first.r.url||url,sourceType:'site_publico',title:title.slice(0,180),emails:c.emails,phones:c.phones,snippet:textContent,scannedAt:new Date().toISOString()});}catch(e){return res.status(502).json({error:e instanceof Error?e.message:'Falha ao consultar página'})}}
